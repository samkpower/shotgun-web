<div class='home'>
  <div class='home__month'>
    {{month-calendar events=events activeDate=activeDate setActiveDate=(action "setActiveDate")}}
  </div>

  <div class='home__day'>
    {{day-calendar events=events activeDate=activeDate newEvent=newEvent}}
  </div>
</div>

{{newEvent.title}} - {{newEvent.start}} - {{newEvent.end}}

<button {{action 'openCreateEventModal'}}>Create Event</button>

{{modals/create-event showModal=showCreateEventModal model=newEvent}}
